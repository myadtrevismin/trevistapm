import { DrapzoneDirective } from './drapzone.directive';

describe('DrapzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DrapzoneDirective();
    expect(directive).toBeTruthy();
  });
});
